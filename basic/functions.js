// a para is what we have here in the parenthesis
function cipher(name, lastname)
//add the + to cat the two strings
 {
    console.log('Hey Jay' + name + lastname);
}
// the arg is the actual value of the parameter
// how we call the function 
cipher('John', 'smith');
// the function can have inputs on it behaves 

//when declaring a function we dont need to add a semi colon