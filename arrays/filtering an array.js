const numbers = [1, 2, -1, 25, 3];

filtered = numbers.filter(function(value) {
  return value >= 0;

});

console.log(filtered);

const filtered = numbers.filter(value => value >= 0;);


const filtered = numbers.filter(n => n >= 0;);
