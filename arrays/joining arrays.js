const numbers = [1, 2, 3];

// when you see a question, the paramter is optional
const joined = numbers.join(',');
console.log(joined)



const message = 'This is my first message';
const parts = message.split(' ');
console.log(parts);

// best for a url slot
const combined = parts.join('-')
console.log(co,combined)
