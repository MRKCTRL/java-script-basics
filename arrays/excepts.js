 const numbers = [1, 2, 3, 4];


 const output = except (numbers, [1]);


 console.log(output);


 function except(array, excluded) {
   let result = numbers.filter(data, idx) => idx != indexToRemove )

   // parse_obj.splice(parse_obj.findIndex(e => e.data,1), 1); //parse_obj is one element less.
   // const removed = .splice(' ')/ []
   // for (let except of array)
   //  if (array[i] === except) {
   //    return except;
}
}
// solution

 function except(array, excluded) {
   const output = []
   for (let element of array)
    if (!excluded.includede(elments))
      output.push(element);
    return output;
}
