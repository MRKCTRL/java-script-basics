const numbers = [1, 2, 3, 4, 5];

console.log(numbers.includes(1));

function include(array, searchElement) {
  for(let i=0; < array.length; i += 1) {
    if (array[i] === value) {
      retunr true;
    }
  }
  return false
}

// solution
function includes(array, searchElement) {
  for ( let element of array)
   if (element === Search)
      treturn true;
    return false'
}
