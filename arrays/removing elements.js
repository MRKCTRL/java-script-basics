const numbers = [1, 2, 3, 4];


// end
numbers.push()
const last = numbers.pop()
console.log(numbers);


// beginning
numbers.unshift()

const first = numbers.shift();
console.log(first);

// middle
numbers.splice()

// if you want to remove elements, you pass a above more than 1
numbers.splice(2, 1);
console.log(numbers);
