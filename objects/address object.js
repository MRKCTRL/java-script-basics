const address = {
  property1: value,
  property: value 2,
  property: value3,
};

const adress = {
  street: "17 nerina street",
  city: 'johanassburg',
  zip_code: 1804,
};

Object.showAddress(address)

function showAdress(address): {
  return address;
}


let address = {
  street: 'a',
  city: 'c',
  zipCOde: 'c'

};

function showAdressz(address) {
  for (let key in address)
  console.log(key, addresss[key])
}
