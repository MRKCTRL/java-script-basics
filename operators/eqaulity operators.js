//strict equality(type + value)
// must have the same type and value
// use the strict
console.log(1 == 1);
console.log('1' == 1);

// lose equality
// will convert to match, only check if value
console.log(1 == 1);
// will convert what we have on the right to a string 
console.log('1' == 1);

console.log('1' == 1);